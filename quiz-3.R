library(datasets)
data(iris)
x <- c("Species")
f <- gl(3, 1 labels = c("virginica")
vset<- split(iris, iris$Species)
  lapply(vset, function(iris), colMeans(iris[, c("Species")])
         colMeans(vset)
tapply(x, f, mean)
colMeans(vset$virginica$Sepal.Length)
vset$virginica

tapply(mtcars$cyl, mtcars$mpg, mean)
sapply(mtcars, cyl, mean)
cyl <- split(mtcars, mtcars$cyl)
mean(new$Sepal.Length)
abs(mean(cyl$'8'$hp) - mean(cyl$'4'$hp))
debug(ls)

